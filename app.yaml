runtime: php
runtime_config:
  document_root: "public"
  operating_system: "ubuntu22"
  runtime_version: "8.2.22"

env: flex


service_account: optical-metric-198312@appspot.gserviceaccount.com

automatic_scaling:
  # Configure automatic scaling here
  max_instances: 1  # This sets the maximum number of instances to 2

env_variables:
  # Put production environment variables here.
  APP_KEY: base64:PA+mxA8BSw2RMOzCT5AiM+sTh9RXcUv+mjb2EInG/OU=
